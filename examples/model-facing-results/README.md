# Model-facing results example

The executable implementation lives in
`supabase/functions/model-facing-results/`. Its integration test asserts the
actual text an MCP client presents to the model.

Run the complete reference check with `pnpm reference:check`.
