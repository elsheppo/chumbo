# Authenticated project tools example

The executable implementation lives in
`supabase/functions/authenticated-tools/`. It exposes `list_projects` and
`create_project` through the connected user's Supabase access token.

Run the complete reference check with `pnpm reference:check`.
