# Many MCPs from one function example

The executable implementation lives in `supabase/functions/many-mcps/`.
`supabase/seed.sql` defines two logical servers with distinct tools.

Run the complete reference check with `pnpm reference:check`.
